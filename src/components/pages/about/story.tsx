import { AboutStory } from '@/src/types/page-content/about-content';
import { JSX } from 'react';

interface Props {
  data: AboutStory;
}
export default function Story({ data }: Props): JSX.Element {
  return <section></section>;
}
